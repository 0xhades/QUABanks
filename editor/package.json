{
  "name": "aounmed-pdf-template-editor",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --hot server.ts",
    "start": "bun run server.ts",
    "check": "bun build server.ts --outfile /dev/null --target bun --format esm",
    "check:builder": "bun build ../build_editable_pptx.mjs --outfile /dev/null --target bun --format esm"
  },
  "dependencies": {
    "jszip": "^3.10.1",
    "pptxgenjs": "^4.0.1"
  }
}
