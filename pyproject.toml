[project]
name = "aounmed-pdf-template-test"
version = "0.1.0"
description = "Isolated Week 1-style question packet renderer experiment"
requires-python = ">=3.11"
dependencies = [
  "arabic-reshaper>=3.0,<4",
  "pypdf>=6.0,<7",
  "python-bidi>=0.6,<1",
  "reportlab>=4.4,<5",
]

[tool.uv]
package = false
